>>> tar [zip, watch, unzip]
tar -cfvz archive.tar.gz [folder/file]
tar -tf archive.tar.gz
tar -xvzf archive.tar.gz -C /tmp

>>> gzip
>>> docker
>>> kubectl

>>> ssh
$ ssh -l vagrant 127.0.0.1 -p 2222 -i /home/ruslan/Desktop/temp/vargant/.vagrant/machines/vm/virtualbox/private_key
